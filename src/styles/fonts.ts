import { Montserrat, Anton, Dancing_Script, Babylonica } from "next/font/google";

export const montserrat = Montserrat({
    subsets: ["latin"],
    weight: ["500"],
    display: "swap",
});

export const anton = Anton({
    subsets: ["latin"],
    weight: ["400"],
    display: "swap",
});

export const dancingScript = Dancing_Script({
    subsets: ["latin"],
    weight: ["600"],
    display: "swap",
});

export const babylonica = Babylonica({
    subsets: ["latin"],
    weight: ["400"],
    display: "swap",
});